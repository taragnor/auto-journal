{
  "name": "auto-journal",
  "version": "1.0.0",
  "description": "Automatic Journalling For Foundry VTT",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -r dist; tsc && copyfiles -u 1 src/**/*.{json,css} dist/ && copyfiles LICENSE README.md dist/"
  },
  "keywords": [
    "foundryvtt",
    "journal"
  ],
  "author": "taragnor",
  "license": "ISC",
  "devDependencies": {
    "@league-of-foundry-developers/foundry-vtt-types": ">=9.280.0",
    "copyfiles": "^2.4.1",
    "typescript": ">=4.8.4"
  }
}
